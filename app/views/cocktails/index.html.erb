<%= render "shared/navbar" %>
<%= render "banner" %>
<div class="cards">
    <% @cocktails.each do |cocktail| %>
  <div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url(https://i.imgur.com/BqNScnf.jpg)">
    <%= link_to "", cocktail_path(cocktail), class: 'card-link' %>
      <%= cocktail.name %>
  </div>
       <% end %>
</div>
